{
  "name": "securelove-monolith",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install --legacy-peer-deps && npm run build && cd ../server && npm install --legacy-peer-deps",
    "install-all": "npm install --legacy-peer-deps && cd client && npm install --legacy-peer-deps && cd ../server && npm install --legacy-peer-deps"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
